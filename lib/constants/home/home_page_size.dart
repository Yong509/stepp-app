import 'package:flutter/material.dart';

class HomePageSize {
  static const addStepPanelBorderRadius = Radius.circular(20);
  static const addStepPanelPadding = EdgeInsets.symmetric(vertical: 26, horizontal: 0);
  static const addStepPanelMenuPlaceSpacing = 30.0;
}
